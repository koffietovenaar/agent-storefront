# Project name

One sentence explaining what this project does and who it helps.

> This is a reusable sample from Koffie Labs. Replace every `{placeholder}`
> before publishing it in a real repository.

## Requirements

- `{runtime}` `{minimum version}`
- `{operating-system or platform constraint}`
- `{external dependency, if any}`

## Install

```sh
{copyable install command}
```

## Quick start

```sh
{smallest successful command}
```

Expected result:

```text
{short representative output}
```

## Configuration

| Name | Required | Default | Purpose |
| --- | --- | --- | --- |
| `{SETTING_NAME}` | Yes | — | `{what it changes}` |

Do not commit credentials. Put safe example values in `.env.example` and keep
real secrets outside the repository.

## Test

```sh
{verified test command}
```

Document the environment in which the command was last run.

## Project structure

```text
.
├── {source-directory}/
├── {test-directory}/
└── README.md
```

## Troubleshooting

### `{specific error message}`

Cause: `{most common concrete cause}`

Fix:

```sh
{recovery command or exact action}
```

## Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md) before opening an issue or pull request.

## License

`{license name and link}`
